# Example state configuration for Scalr
# Copy to state.config in your environment directory and replace with your values
# This file should NOT be committed to git

# Note: workspace name should be hardcoded in the main.tf file
# Only hostname and organization are passed through this file
hostname     = "username.scalr.io"
organization = "env-123456789"